[tool.ruff]
target-version = "py310"
line-length = 100
extend-select = ["I"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
addopts = "--asyncio-mode=auto --maxfail=1 --cov=latentscore --cov-report=term-missing"
testpaths = ["tests"]
