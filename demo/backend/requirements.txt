# NOTE: torch + torchvision are installed (CPU-only, pinned) in
# demo/backend/Dockerfile BEFORE this file is processed, so they are
# intentionally NOT listed here -- listing them would risk pip resolving the
# CUDA build. The CPU index below is a safety net for any transitive torch pin.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.12
uvicorn[standard]==0.34.2
laion-clap          # fast_heavy model path (pulls torchvision; already satisfied)
